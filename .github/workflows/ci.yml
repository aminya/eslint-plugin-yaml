name: CI
on:
  - push
  - pull_request

jobs:
  Test:
    if: '!contains(github.event.head_commit.message, "[skip ci]")'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
<<<<<<< HEAD
          node-version: "14.x"
=======
      asdasdasd node-version: "14.x"
>>>>>>> 64d22f94165e94a35c8970493d68d98cdebb8ec1
      - name: Install NPM dependencies
        run: |
          npm ci  # uses locked module. use `npm install` for non-locked
          npm run test-link
      - name: Run tests 👩🏾‍💻
        run: npm run test
      - name: Lint ✨
        run: npm run lint

  Skip:
    if: contains(github.event.head_commit.message, '[skip ci]')
    runs-on: ubuntu-latest
    steps:
      - name: Skip CI 🚫
        run: echo skip ci
